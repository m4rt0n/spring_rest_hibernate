package com.application.hibernate_spring.testweb;

import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
public class IntegrationTest {

	/*
	 * @Test public void
	 * givenUserDoesNotExists_whenUserInfoIsRetrieved_then404IsReceived() throws
	 * ClientProtocolException, IOException {
	 * 
	 * // Given String name = RandomStringUtils.randomAlphabetic(8); HttpUriRequest
	 * request = new HttpGet("http://localhost:8081/person/getbyname/" + name);
	 * 
	 * // When HttpResponse httpResponse =
	 * HttpClientBuilder.create().build().execute(request);
	 * 
	 * // Then assertEquals(HttpStatus.SC_NOT_FOUND,
	 * httpResponse.getStatusLine().getStatusCode()); }
	 */
}
